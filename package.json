{
  "name": "davidlloyd-price-index",
  "version": "1.0.0",
  "private": true,
  "description": "Rack Rate — unofficial David Lloyd price lookup (static site).",
  "homepage": "https://dylnyko.github.io/davidlloyd-price-index",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "serve": "python3 -m http.server 4173"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1"
  }
}
