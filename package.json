{
  "name": "davidlloyd-price-index",
  "version": "1.0.0",
  "private": true,
  "description": "The Price Book — unofficial David Lloyd price lookup (static site).",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "serve": "python3 -m http.server 4173"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1"
  }
}
